function[avg_perf] = performance_avg(study_nm, subject_id, condition)
%[avg_perf] = performance_avg(study_nm, subject_id, condition)
% performance_avg will look at the average performance for each task of
% each subject
%
% INPUTS
% study_nm: study name
%
% subject_id: list of subjects
%
% condition: condition
%
% OUTPUTS
% avg_perf: average performance

end % function